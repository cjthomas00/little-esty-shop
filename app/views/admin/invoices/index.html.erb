<%= render 'admin/dashboards/admin' %>

<section>
  <div class = "invoices">
  <h2>Invoices</h2>
    <ul>
    <% @invoices.each do |invoice| %>
      <%# <li>Invoice #<%= link_to invoice.id, admin_invoices_path(invoice) </li> %>
    <li>Invoice #<%= link_to "#{invoice.id}", "/admin/invoices/#{invoice.id}" %> </li>
      <% end %>
    </ul> 
   </div> 
 </section> 